{
  "name": "@clawflare/cloudflare",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare API egress handler for Clawflare",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "node --import=tsx **/*.test.ts"
  },
  "dependencies": {
    "@clawflare/egress-core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
